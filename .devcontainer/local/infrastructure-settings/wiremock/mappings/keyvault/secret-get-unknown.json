{
  "priority": 8,
  "request": {
    "method": "GET",
    "urlPathPattern": "/secrets/[^/]+(/[^/]*)?"
  },
  "response": {
    "status": 404,
    "headers": { "Content-Type": "application/json" },
    "jsonBody": {
      "error": {
        "code": "SecretNotFound",
        "message": "A secret with (name/id) {{request.pathSegments.[1]}} was not found in the wiremock vault - seed it in .devcontainer/local/infrastructure-settings/wiremock/mappings/ if a local flow needs it."
      }
    }
  }
}
