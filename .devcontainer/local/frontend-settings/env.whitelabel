# .devcontainer/frontend/env.whitelabel
# Local-stack overrides for MspProcess.WhiteLabel.Web (mspprocess-ui/Web/MspProcess.WhiteLabel.Web),
# sourced by .devcontainer/local-stack-frontend.sh before it
# launches this app's vite dev server.
# Derived from that repo's Web/MspProcess.WhiteLabel.Web/config/.env.Development.
REACT_APP_BASE_API_URL='http://localhost:5000'
REACT_APP_MSPP_FRONTEND_URL='http://localhost:3000'
