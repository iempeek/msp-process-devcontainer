# .devcontainer/frontend/env.client-portal
# Local-stack overrides for AlertManager.ClientPortal.Web (mspprocess-ui/Web/AlertManager.ClientPortal.Web),
# sourced by .devcontainer/local-stack-frontend.sh before it
# launches this app's vite dev server.
# Derived from that repo's Web/AlertManager.ClientPortal.Web/config/.env.Development.
# REACT_APP_URL points at AlertManager.LiveChat.Api (port 5033 locally, see
# local-stack.sh --run) - NOT the main AlertManager.Api on 5000.
REACT_APP_URL='http://localhost:5033/'
REACT_APP_AUTH_URL='http://localhost:3001'
